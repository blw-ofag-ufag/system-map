PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

CONSTRUCT {
  ?instance rdf:type ?parentClass .
}
WHERE {
  ?instance rdf:type ?childClass .
  ?childClass rdfs:subClassOf+ ?parentClass .
}
